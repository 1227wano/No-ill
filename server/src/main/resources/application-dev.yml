spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource:
    hikari:
      maximum-pool-size: 2

logging:
  level:
    root: INFO
    com.noill: DEBUG
    org.hibernate.SQL: DEBUG
